apiVersion: v1
kind: ServiceAccount
metadata:
  name: fastapi-app-sa
  namespace: fastapi-app
  labels:
    app.kubernetes.io/name: fastapi-app
# Disable automatic token mounting — only mount if your app calls the K8s API
automountServiceAccountToken: false
