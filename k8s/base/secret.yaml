# SENIOR NOTE: Never commit real secrets. This is a placeholder.
# In production use: Sealed Secrets, External Secrets Operator, or Vault Agent.
apiVersion: v1
kind: Secret
metadata:
  name: fastapi-secrets
  namespace: fastapi-app
  annotations:
    # Signals that this must be replaced by real secret management
    sealedsecrets.bitnami.com/managed: "true"
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:REPLACE_ME@postgres-svc:5432/appdb"
  SECRET_KEY: "REPLACE_ME_WITH_32_CHAR_RANDOM_STRING"
